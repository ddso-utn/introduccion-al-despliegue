proxy_cache_path /var/cache/nginx levels=1:2 keys_zone=app_cache:1m inactive=5m;

server {
    listen 80;

    location / {
        proxy_pass http://app:3000;
        proxy_cache app_cache;
        proxy_cache_valid 200 5m;
        add_header X-Cache-Status $upstream_cache_status;
    }
}
